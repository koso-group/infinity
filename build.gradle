plugins
{
    id 'java'
    id 'maven-publish'
}


publishing {
    repositories {
        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/koso-group/infinity")
            credentials {
                username = System.getenv("USERNAME")
                password = System.getenv("TOKEN")
            }
        }
    }
    publications {
        gpr(MavenPublication) {
            from(components.java)
        }
    }
}


group 'kosogroup'
version '1.0.0'

sourceCompatibility = 1.8

repositories
{
    mavenCentral()
}

dependencies
{
}
